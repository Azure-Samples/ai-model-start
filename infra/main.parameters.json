{
  "$schema": "https://schema.management.azure.com/schemas/2019-04-01/deploymentParameters.json#",
  "contentVersion": "1.0.0.0",
  "parameters": {
    "environmentName": {
      "value": "${AZURE_ENV_NAME}"
    },
    "location": {
      "value": "${AZURE_LOCATION}"
    },
    "deploymentName": {
      "value": "${AZURE_MODEL_DEPLOYMENT_NAME=DeepSeek-R1-0528}"
    },
    "deployment2Name": {
      "value": "${AZURE_MODEL_2_DEPLOYMENT_NAME=gpt-4.1-mini}"
    }
  }
}
